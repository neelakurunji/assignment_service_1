kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: assignment-service-1-pvc
spec:
  storageClassName: assignment-service-1-storageclass
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi